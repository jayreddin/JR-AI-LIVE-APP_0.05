Yes I'll share you the app modifications and implementations I want to add to the code files.

1. I want good API Docs fetched from the web for some features and they will have to be fetched, web scrapped or manually copied using The MCP Server available: fetch, context7 for api docs, browsermcp, playwright and puppeteer for web scraping or visiting to copy and paste all the API Doc pages, have the modes called use the mcp servers available or if they need a specific sever they can get the MCP mode specialist to implement the new server.

2. I will provide you with an API doc file I already have complete and we'll use it later, its the puter_api_docs.md file and I'll add it to the knowledgebase folder, so this single puter.com exact URL address can be skipped:

https://docs.puter.com/prompt.md 

3. When a mode is scraping a webpage for API docs or manually browsing each page in a browser ensure they search for and figure out a the list of links to the next pages of the API docs. They Should add that list of URLs into  in order into its own .md file named Example: Puter-Sitemap.md. then from that they can visit each of those URLs And retrieve the various API docs.

First URL to investigate for links to other api doc pages:

https://docs.puter.com/

I managed to find a docs.puter.com sitemap and will add every page to find in a list in a new file in the knowledgebase folder called docs.puter.sitemap.md. have a mode review it before they go to retrieve the API docs from the URL. In the file I provided I added an * beside section titles and listed under them is all the various api docs relevant, each of those sections is with the astrix is the name of the new api file and the file will contain all the API Docs for that section, there will be multiple APi Doc files.

The Next URL/ Knowledgebase Api files to be added is the puter code examples tutorial pages, I have listed the link names in a second new file called: tutorials.puter.sitemap.md

Now have One of the research and Discovery type of modes for and retrieve the API docs from the 2 puter URLs from the sitemap files.

When these 2 are complete I will give you more API Docs URLs to retrieve web page api content  for geminiAI and also instructions. Start to implement the puter api docs retrieval.


Start with the tutorial.puter.sitemap.md and extract 